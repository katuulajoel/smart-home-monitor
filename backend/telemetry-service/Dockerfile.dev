FROM node:20-alpine

WORKDIR /app

# Copy package manifests
COPY package*.json ./
COPY backend/shared/package.json ./backend/shared/
COPY backend/telemetry-service/package.json ./backend/telemetry-service/

# Copy source BEFORE install so lifecycle scripts can run
COPY backend/shared ./backend/shared
COPY backend/telemetry-service ./backend/telemetry-service

# Install deps (this will now succeed because tsconfig + sources exist)
RUN npm install --workspaces --include-workspace-root

# Default workdir
WORKDIR /app/backend/telemetry-service

CMD ["npm", "run", "dev"]
